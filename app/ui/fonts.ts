import { Inter, Lunasima } from 'next/font/google'

// 拉丁字体
export const inter = Inter({ subsets: ['latin'] })

export const lusitana = Lunasima({
    weight: [ '400', '700' ],
    subsets: [ 'latin' ]
})
